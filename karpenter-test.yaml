apiVersion: v1
kind: Pod
metadata:
  name: karpenter-test
spec:
  containers:
  - name: pause
    image: public.ecr.aws/eks-distro/kubernetes/pause:3.7
    resources:
      requests:
        cpu: "1"
        memory: "1Gi"
  nodeSelector:
    role: system
